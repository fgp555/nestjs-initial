{
  "scripts": {
    "dev": "npm run dev --prefix frontend/reactjs",
    "build:frontend": "npm run build --prefix frontend/reactjs",
    "build:backend": "npm run build --prefix backend",
    "build:all": "npm run build:backend && npm run build:frontend",
    "deploy": "npm run build:all && git add . && git commit --amend --no-edit && git push --force"
  }
}
